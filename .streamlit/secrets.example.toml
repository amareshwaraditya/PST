# Local PostgreSQL configuration (safe to commit).
#
# Copy this file to `.streamlit/secrets.toml`, then set the password for your
# local PostgreSQL instance. That real secrets file is ignored by Git.
DATABASE_URL = "postgresql://postgres:postgres@localhost:5432/pst"